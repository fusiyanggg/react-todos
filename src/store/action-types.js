/**
 * 增加一个待办事项
 * @type {string}
 */
export const ADD_TODO = 'ADD_TODO';
/**
 * 修改过滤类型 all（显示全部） active（显示未完成）
 * completed（显示已完成的）;
 * @type {string}
 */
export const CHANGE_FILTER = 'CHANGE_FILTER';